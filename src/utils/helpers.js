export const helpers = {

    // !this method deprecated
    concateFullName: (data) => {
        return data
    },

    // * this method can be improved
    dateFormate: (date) => {
        return date
    }
}